#include "main.h"

/**
 * mul - recibe dos enteros y los multiplica
 * @a: entero a multiplicar por la funcion
 * @b: entero a multiplicar por la funcion
 * Return: resultado de la multiplicacion
 */
int mul(int a, int b)
{
	int mul = a * b;
return (mul);
}
